Boilerplate for building new frontend projects
=====================================

To run builder:

1. install npm dependencies:

	> npm i

1. install bower dependencies:

	> bower i

2. start a builder using default gulp task

	> gulp
